class Person {
    constructor (name, job, age) {
        this.name = Laura;
        this.job = backendDeveloper ;
        this.age = 20;
        this.fetchJob = true;
    }

    sayName() {
        console.log(`${this.name}`);
        console.log(`${this.name} is a ${this.job}`)
        this.fetchJob = true;
    }
}